namespace Colors {
  // Prefer full 6-digit colors here. Sheets normalizes to these and it makes
  // checks in tests easier.
  export const RED_BALANCE = '#bb3344';
  export const GREEN_BALANCE = '#119922';
}
